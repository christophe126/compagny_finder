<div class="container mt-5">
  <h1 class="text-center">Liste des entreprises</h1>

  <div class="form-inline">
    <%#= render 'form_search' %>
    <%= @resultats %>
    <%= ENV['APIAUTHORIZATION'] %>
  </div>
  <%# if !@result.nil? %>
  <H3 class="mt-4">Nombre de résultats : <%#= @result["etablissement"].count %></H3>
  <ul>
    <%# @result["etablissement"].each do |etab| %>
    <li><%#= etab["nom_raison_sociale"] %> </li>
    <%# end %>
  </ul>
  <%# end %>
</div>
